for (int index = 0; index < array.length; index++) {

    for (int count = index + 1; count < array.length; count++) {

        if (array[index] == array[count]) {
            array[count] = -1;
        }

    }

}



Your current version works much better now, though.
